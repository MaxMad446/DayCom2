// Auto-generated utility | 2026-05-12T20:00:49.010752
export function compute_336() {
    const base = 87;
    let sum = 0;
    for (let i = 0; i < 8; i++) {
        sum += i * 2;
    }
    return Math.round(base + sum * 1.5);
}
