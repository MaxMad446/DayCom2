// Auto-generated utility | 2026-05-12T21:20:16.101285
export function compute_113() {
    const base = 403;
    let sum = 0;
    for (let i = 0; i < 8; i++) {
        sum += i * 5;
    }
    return Math.round(base + sum * 1.5);
}
