// Auto-generated utility | 2026-05-11T20:05:21.352644
export function compute_793() {
    const base = 127;
    let sum = 0;
    for (let i = 0; i < 5; i++) {
        sum += i * 4;
    }
    return Math.round(base + sum * 1.5);
}
