// Auto-generated utility | 2026-05-12T20:42:13.575273
export function compute_288() {
    const base = 14;
    let sum = 0;
    for (let i = 0; i < 21; i++) {
        sum += i * 2;
    }
    return Math.round(base + sum * 1.5);
}
