// Auto-generated utility | 2026-05-12T21:29:30.587705
export function compute_952() {
    const base = 413;
    let sum = 0;
    for (let i = 0; i < 14; i++) {
        sum += i * 2;
    }
    return Math.round(base + sum * 1.5);
}
